public class SalesforceDeveloper {
public void Car(){
    string brand = 'Honda';
    string model = 'Insight';
    string color = 'Grey';
    string myCar = brand+' '+ model+' '+ color;
    system.debug(brand);
    system.debug(model);
    system.debug(color);
    system.debug(myCar);
}
}